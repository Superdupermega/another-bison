# Fence pack: payments (paid, layout preview)

Vertical packs follow the core policy schema and add regulation-traceable policies.
Each policy carries a `regulatory_mapping` list (e.g. `PSD2 Art. 73`, `PCI DSS 3.4`)
so the traceability matrix from regulation to enforcement point is generated, not
written. Contents are delivered under engagement or licence.
