{
  "name": "bison-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bison — intent engineering for AI-native software. Static site plus Vercel serverless API backed by Neon Postgres.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs 8080",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  }
}
